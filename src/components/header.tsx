import { useState } from "react"

export function Header() {
  return (
    <div className="absolute top-0 left-0 w-full p-10 h-10 flex items-center justify-center">
        <h1 className="text-md opacity-50">Math Logic Statements and Quantifiers</h1>
    </div>
  )
}